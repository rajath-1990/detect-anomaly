<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ requires this at runtime, not just here. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_dialog_alert"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImpossibleTravel">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ImpossibleTravel"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Only fires while the app is in the FOREGROUND. Backgrounded, the
             system tray handles the notification payload without calling us. -->
        <service
            android:name=".AnomalyMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Without this, FCM auto-creates a DEFAULT-importance channel and the
             notification never pops as a heads-up banner, no matter what
             priority the backend sends. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="anomalies" />

    </application>
</manifest>
